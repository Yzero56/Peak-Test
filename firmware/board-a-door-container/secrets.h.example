// secrets.h.example — secrets.h로 복사한 뒤 실제 값을 채우세요.
// secrets.h는 커밋되지 않습니다 (.gitignore 참고).
#pragma once

// 시연장/집 공유 와이파이(STA). 비워두면(빈 문자열) AP 핫스팟만 사용.
#define WIFI_STA_SSID     ""
#define WIFI_STA_PASSWORD ""

// STA 실패 시(또는 항상 병행) 뜨는 자체 핫스팟
#define AP_SSID     "FridgeCam"
#define AP_PASSWORD "FridgeCamTest"
