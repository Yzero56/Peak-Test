.venv/
__pycache__/
*.pyc
.env
data/raw_captures/
data/raw_captures_sidebyside_archive/
data/raw_captures_excluded_lowsignal/
data/exports/
data/inout_sessions/
data/rgb_pairs/
data/events/
data/category_eval_captures/

# Claude Code 로컬 스킬/설정 — 팀 저장소에 넣을 코드가 아님
.claude/
build/
*.bin
# 학습된 모델 자체(model.eim)는 저장소에 포함한다 — API 키 없이도 실행 가능하게 하기 위함.
# 시행착오 중 만든 백업 사본(model.eim.bak_*)만 제외.
*.eim.bak_*
.DS_Store
tmp_preview/
weights/
*.pt

# 와이파이 비밀번호가 채워질 수 있는 펌웨어 — 추적 해제(git rm --cached 완료), 실수로 커밋되지 않게 예방
firmware/webcam_ap_capture/webcam_ap_capture.ino
